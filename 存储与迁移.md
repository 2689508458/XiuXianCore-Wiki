# 存储与迁移

> 这页解决：插件数据存在哪里、怎么配置 MySQL、以及如何在 YAML / SQLite / MySQL 之间搬家。

## 三种存储后端

| 类型 | 数据位置 | 适用场景 |
|---|---|---|
| `SQLITE`（默认） | `plugins/XiuXianCore/数据.db` | 绝大多数服务器，零配置开箱即用 |
| `YAML` | `plugins/XiuXianCore/数据/玩家/<uuid>.yml`、`数据/globals.yml` | 小服调试、想直接肉眼看数据 |
| `MYSQL` | 你的 MySQL 5.7+ 数据库 | 有独立数据库、需要备份/外部查询的服 |

存的内容：玩家境界、击杀计数、玩家变量、全局变量。

## config.yml 配置

```yaml
storage:
  type: SQLITE          # YAML | SQLITE | MYSQL
  auto-save-seconds: 300
  mysql:
    host: 127.0.0.1
    port: 3306
    database: xiuxian
    user: root
    password: ""
    table-prefix: xiuxian_
    pool-size: 8
```

| 字段 | 类型 | 默认 | 说明 |
|---|---|---|---|
| `type` | 文本 | `SQLITE` | 存储类型，改动后**需重启**（重载不切换后端） |
| `auto-save-seconds` | 整数 | `300` | 自动保存间隔（秒），最小 10 |
| `mysql.host` / `port` | 文本/整数 | `127.0.0.1` / `3306` | 数据库地址 |
| `mysql.database` | 文本 | `xiuxian` | 库名；不存在时插件会**自动创建**（需账号有建库权限），也可预先 `CREATE DATABASE xiuxian DEFAULT CHARSET utf8mb4;` |
| `mysql.user` / `password` | 文本 | `root` / 空 | 账号密码 |
| `mysql.table-prefix` | 文本 | `xiuxian_` | 表前缀，多插件共库时防冲突 |
| `mysql.pool-size` | 整数 | `8` | 连接池大小 |

## MySQL 要求与说明

- 版本 **5.7 及以上**；表由插件自动创建（utf8mb4 字符集，键列 VARCHAR(191) 兼容 5.7 索引长度）
- 驱动已内置在插件里，**不需要**额外安装任何前置
- **连接失败不会崩服**：插件会在控制台醒目警告并临时回退到 SQLITE；此时请修好配置后重启（回退期间产生的数据在 SQLite 里，必要时用迁移指令搬回）
- 用 `/xiuxian version` 可随时查看"配置的后端"与"实际生效的后端"是否一致

## 保存机制

- 数据改动先进内存，按 `auto-save-seconds` 周期异步落盘（默认 5 分钟）
- 玩家退出时立即保存该玩家；关服时全量保存
- 玩家进服时异步载入，载入完成前该玩家的突破类操作会被拒绝（保护数据）

## 存储迁移

```
/xiuxian admin migrate <源> <目标>
```

例：`/xiuxian admin migrate SQLITE MYSQL`

- 源/目标可填 `YAML` `SQLITE` `MYSQL`，不能相同
- **必须在无玩家在线时执行**（控制台执行即可）
- 迁移是"全量复制"，不会删除源数据，也**不会自动改配置**
- 迁移完成后：把 `config.yml` 的 `storage.type` 改成目标类型，然后**重启服务器**

## 相关管理指令

| 指令 | 说明 |
|---|---|
| `/xiuxian admin data <玩家名\|UUID>` | 查看某玩家的存储数据（在线读缓存，离线读库） |
| `/xiuxian admin var set global <键> <值>` | 设置全局变量（键仅限英文/数字/下划线） |
| `/xiuxian admin var get global <键>` | 查看全局变量 |
| `/xiuxian admin migrate <源> <目标>` | 存储迁移 |

> 玩家变量与四则运算表达式修改将在条件系统上线时（阶段3）提供。
