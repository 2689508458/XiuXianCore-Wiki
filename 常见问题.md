# 常见问题

**Q：配置里的中文显示乱码？**
用 UTF-8 编码保存配置文件（VSCode/Notepad++；别用记事本的 ANSI）。Linux 服务器需启用 UTF-8 locale（中文文件夹名依赖它）。

**Q：控制台提示"存储后端 MYSQL 不可用，已临时回退到 SQLITE"？**
MySQL 连不上（地址/密码/防火墙）。修好 config 后重启；回退期间产生的数据在 SQLite 里，可用 `/xiuxian admin migrate SQLITE MYSQL` 搬回。

**Q：境界属性没生效？**
① 确认装了 AttributePlus 且 `/xiuxian version` 显示已挂接；② `attributes` 的属性名必须与你 AP 配置的属性名**完全一致**（默认中文名，如 `攻击力`）；③ 改完 `/xiuxian admin reload` 会自动刷新在线玩家。

**Q：击杀 MM 怪不计数？**
`kill mm:<id>` 的 id 是 MM **内部名**（`/mm mobs list` 查看，大小写一致），不是显示名。

**Q：渡劫失败为什么扣了两笔？**
默认损失 = 突破消耗（境界文件 `costs`，天劫降临时扣）+ 失败惩罚（天劫文件 `penalty.levels`）。想失败只扣惩罚，把天劫的 `pay-costs` 改为 `on-success`。

**Q：`{progress}` 和 `%xiuxian_progress%` 有什么区别？**
`{xxx}` 是本插件配置内部占位（条件表达式/动作/界面文本）；`%xiuxian_xxx%` 是给其他插件（计分板/菜单/AX）的 PAPI 占位符。两套都有进度、境界、变量等。

**Q：改了配置没生效？**
绝大多数配置 `/xiuxian admin reload` 即时生效；唯一例外是 `config.yml` 的 `storage` 段（换存储后端需重启）。

**Q：玩家进服后马上操作提示"数据尚未加载完成"？**
数据是异步载入的（通常毫秒级）。若频繁出现，检查存储后端延迟（MySQL 网络）。

**Q：想禁用自带示例（境界/界面等）？**
直接删除或修改释出的文件即可；想保持文件夹为空又不想重新释出示例，放一个 `_占位.yml`（`_` 开头的文件会被忽略）。

**Q：支持 WorldGuard 渡劫区域吗？**
暂不支持命名区域；用坐标圈：天劫文件 `restrict.regions: ["world,100,64,200,20"]`（世界,x,y,z,半径）。
