# 安装与前置

> 这页解决：装插件需要什么环境、配哪些前置、装完第一次启动会发生什么。

## 环境要求

| 项 | 要求 |
|---|---|
| 服务端 | Paper / Purpur **1.21.4**（本插件在 Purpur 1.21.4-2416 上开发测试） |
| Java | **21 或更高** |
| 前置插件 | 无硬性前置——全部为软依赖，缺了对应功能自动停用，不会报错崩服 |

## 前置插件（按需安装）

| 插件 | 缺少时的影响 | 推荐度 |
|---|---|---|
| AttributePlus 3.3.3+ | 境界属性加成不生效 | ★★★ |
| PlaceholderAPI | `%xiuxian_*%` 占位符与文本解析不可用 | ★★★ |
| MythicMobs 5.x | MM 击杀条件 / MM 物品条件与给予 / 渡劫召唤不可用 | ★★★ |
| Vault + 任一经济插件 | `money` 金币条件/消耗不可用 | ★☆☆ |
| PlayerPoints | `points` 点券条件/消耗不可用 | ★☆☆ |
| ArcartX v2 | AX 自定义界面对接不可用 | 按需 |

> WorldGuard 目前没有对接（渡劫地点用插件自带的坐标圈方案，见《渡劫配置》），无需为本插件安装。

> 启动后随时可用 `/xiuxian version` 查看各前置的挂接状态。

## 安装步骤

1. 把 `XiuXianCore-x.y.z.jar` 放入服务端 `plugins/` 目录
2. 按需放入上表前置插件
3. 启动服务器

## 首次启动生成的目录

```
plugins/XiuXianCore/
├── config.yml       # 全局配置（见《配置总览》）
├── messages.yml     # 全部消息文案
├── 境界/            # 境界配置（首次生成 炼气期/筑基期/金丹期 示例）
├── 渡劫/            # 天劫配置
├── 变量/            # 变量定义
├── 界面/            # 箱子 GUI
├── AX界面/          # ArcartX 对接
├── 动作组/          # 可复用动作组（1.3.0 起，见《突破与条件》）
├── 灵脉/            # 修炼加成区域（1.4.0 起，见《打坐修炼》）
└── 数据.db          # SQLite 数据文件（默认存储）
```

- 控制台看到 `境界链已加载: 9 个节点` 与 `已挂接 AttributePlus` 即为正常
- 快速上手：进服后输入 `/xiuxian info` 查看自己的境界（新玩家自动为链首境界）

## 依赖自动下载（1.2.0 起）

插件本体只有约 0.2MB——数据库驱动等三个库由**服务端首次加载插件时自动从 Maven 中央仓库下载**（控制台会显示 Downloading library）。国内服务器如下载缓慢，在启动命令加一个参数切换到阿里镜像：

```
-Dorg.bukkit.plugin.java.LibraryLoader.centralURL=https://maven.aliyun.com/repository/central
```

（加在 `java` 与 `-jar` 之间；只需下载一次，之后走本地缓存。）

## 常见问题

- **Linux 服务器**：本插件的配置目录使用中文名（境界/渡劫/……），系统需启用 UTF-8 locale（绝大多数发行版默认满足）
- **AttributePlus 是 TabooLib 插件**：它第一次启动会联网下载运行库，属正常现象
