# 变量系统

> 这页解决：怎么定义变量、怎么读写、怎么在条件/表达式/占位符里使用。

## 概念

- **玩家变量**：每个玩家一份（如 灵根、灵气），持久化存储
- **全局变量**：全服一份（如 活动开关、世界灵气浓度）
- 值按文本存储，参与运算时自动转数值；可以存字符串（字符串变量只支持 set/get）

## 定义变量（`变量/` 文件夹，每文件一个）

```yaml
# 变量/灵根.yml
id: linggen          # 英文 id，引用时用它
display: "灵根"       # 进度文本等处的显示名
initial: 1           # 未设置时的默认值
min: 0               # 可选：数值下限（写入时自动收拢）
max: 10              # 可选：数值上限
description: "资质：决定渡劫成功率等加成"
sync-arcartx: true   # 是否同步到 ArcartX 客户端
```

- 首次启动自动释出示例：灵根（linggen）、灵气（lingqi）
- 不在 `变量/` 里定义的 id 也能直接使用（无默认值/无上下限），但建议都定义，便于管理

## 修改变量

```
/xiuxian admin var <set|add|take|get> <玩家|global> <变量id> [值或表达式]
```

例子：

```
/xiuxian admin var set  Steve linggen 5
/xiuxian admin var add  Steve lingqi {level} * 10      （表达式按目标玩家求值）
/xiuxian admin var take Steve lingqi 100
/xiuxian admin var set  global world_lingqi (3+4)*2
/xiuxian admin var get  Steve linggen
```

- `add`/`take` 要求当前值是数字；`set` 的表达式无法求值时按文本原样存储
- `min`/`max` 对玩家变量与全局变量都生效（写入时自动收拢到范围内）
- 修改会触发 `PlayerVariableChangeEvent`（供其他插件监听）
- 其他插件/任务系统可通过控制台执行以上指令发放变量（如击杀奖励灵气）

### 在动作里直接改变量（无需 console 指令）

突破/渡劫/界面按钮的动作列表里可直接写（作用于触发动作的玩家）：

```yaml
on-success:
  - "var: add lingqi {level} * 10"     # 玩家变量：set/add/take
  - "gvar: add world_lingqi 1"         # 全局变量同理
```

## 实战闭环示例：灵气经济

1. **发放**——MM 怪死亡技能 / 任务插件奖励里执行控制台命令：
   `xiuxian admin var add <player.name> lingqi 50`
2. **门槛**——境界突破条件：`var linggen >= 3`
3. **消耗**——突破消耗：`var lingqi 500`
4. **加成**——天劫成功率：`success-chance: "60 + {var:linggen} * 5"`（灵根越高越稳）
5. **展示**——计分板 `%xiuxian_var_lingqi%`，AX 界面 `xx_var_lingqi`

## 在哪里使用

| 场景 | 写法 |
|---|---|
| 突破条件 | `var linggen >= 3` |
| 突破消耗 | `var lingqi 1000` |
| 表达式 | `{var:linggen}`、`{gvar:world_lingqi}` |
| PAPI 占位符 | `%xiuxian_var_linggen%`、`%xiuxian_gvar_world_lingqi%` |
| 渡劫成功率 | `success-chance: "60 + {var:linggen} * 5"` |
| 动作 DSL | `var: add lingqi 100`、`gvar: set world_lingqi 0` |
