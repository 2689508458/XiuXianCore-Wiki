# 境界配置

> 这页解决：怎么写一个境界文件、大境界/小境界怎么展开成境界链、属性加成怎么配。

## 基本概念

- `境界/` 文件夹里一个文件可以写**一个**大境界（顶层 `id:`）或**多个**大境界（顶层键即 id）——两种写法与示例见《[配置总览](配置总览.md)》"配置文件的两种写法"；文件名随意（仅组织用途）
- 全服境界按 `order` 值从小到大连成一条**境界链**，新玩家自动处于链首节点
- 大境界可以配 `sub-realms`（小境界），会自动展开为链上的多个节点：
  - 节点 ID = `大境界id_小境界id`，例如 `zhuji_qianqi`（筑基期·前期）
  - 不配 `sub-realms` 时，该文件本身就是一个节点（节点 ID = `id`）

## 完整字段说明

```yaml
id: zhuji               # 必填。英文/数字/下划线；玩家数据里存的就是它
order: 20               # 必填。链上排序，数值留间隔（10,20,30...）便于以后插入新境界
display: "&b筑基期"      # 展示名，支持颜色
description:            # 描述（GUI 中展示）
  - "&7引气入体，筑就道基"
icon: IRON_INGOT        # 图标材质（GUI 用），支持 材质 或 材质:自定义模型数据

sub-realms:             # 可选。小境界列表，按书写顺序展开
  - id: qianqi
    display: "&b筑基期·前期"
  - id: zhongqi
    display: "&b筑基期·中期"
  - id: houqi
    display: "&b筑基期·后期"
    # 小境界里可以写 attributes / breakthrough / cultivation / icon / description 覆盖大境界的默认值

attributes:             # AttributePlus 属性加成，进入该境界自动应用
  - "攻击力: 15"
  - "生命上限: 60"

cultivation:            # 可选。覆盖该境界的全局打坐收益动作（v1.4.0）
  reward-actions:
    - "var: add lingqi 2 * {multiplier}"

next-realm: jindan_qianqi   # 可选。显式指定下一境界（默认走链上下一个节点）

breakthrough:           # 突破段（条件/消耗/动作在阶段3启用，可先照示例写好）
  conditions: [ "level >= 15" ]
  costs: [ "level 15" ]
  success-chance: "80 + {var:linggen} * 2"  # 可选。百分比表达式，省略为 100
  on-success: [ "message: &a突破成功！", "sound: ENTITY_PLAYER_LEVELUP" ]
  on-fail: [ "message: &c条件未满足。" ]
  on-chance-fail: [ "message: &c突破失败，道基受损……", "chance: 30", "demote: 1" ]
```

> `on-chance-fail` 从 v1.4.1 起可用；省略时概率失败兼容回退到 `on-fail`。升级说明见《[v1.4.0 / v1.4.1 更新指南](v1.4.0更新指南.md)》。

## 属性加成（AttributePlus）

- `attributes` 每行格式与 AP 物品 lore 属性一致：`"属性名: 数值"`，**属性名必须与你服务器 AP 的属性名完全一致**（AP 默认就是中文属性名）
- 应用方式为 AP 的"来源属性"，来源 ID 固定 `xiuxian_realm`——在 AP 属性面板里能看到这一来源
- 切换境界时自动移除旧境界属性并应用新属性；小境界不写 `attributes` 时继承大境界的
- 未安装 AP 时此项自动跳过

## 规则与排错

| 现象 | 原因 |
|---|---|
| 控制台提示"缺少合法 id" | `id` 为空或含中文/横线等非法字符 |
| 提示"节点 ID 重复" | 两个文件/小境界展开出了相同节点 ID |
| 提示"order 冲突" | 两个大境界 `order` 相同，链顺序不可预期 |
| 玩家境界显示"未知" | 玩家数据里的境界节点在当前配置中被删除/改名了，用 `/xiuxian admin setrealm` 修正 |

- 改完文件 `/xiuxian admin reload` 即时生效，并会给在线玩家重新应用属性
- `/xiuxian admin realms` 可随时查看展开后的完整境界链
