# 界面配置（箱子 GUI）

> 这页解决：怎么自定义箱子界面的布局、物品和点击动作。纯原版客户端可用，无需任何 MOD。

## 基本用法

- `界面/` 文件夹每文件一个界面；首次启动释出默认 `主界面.yml`（id `main`）和 `境界一览.yml`（id `realm_list`）
- `/xiuxian` 无参打开 `main`；`/xiuxian gui <id>` 打开任意界面；改完文件 `/xiuxian admin reload` 生效（会关闭玩家已打开的旧界面）

## 界面文件结构

```yaml
id: main                     # 英文 id
title: "&8【&b修仙&8】境界面板"   # 标题，支持占位符
refresh-interval: 20         # 可选：>0 时每隔 N tick 自动重渲染（进度实时跳动）；0/省略 = 只在点击后刷新
layout:                      # 1~6 行字符画，每行最多 9 格；空格 = 空槽位
  - "#########"
  - "#   A   #"
  - "# B C D #"
  - "#########"
items:
  "#":                       # 键 = layout 里的符号（单字符）
    material: BLACK_STAINED_GLASS_PANE
    name: " "
  "C":
    material: NETHER_STAR    # 材质；支持 材质:自定义模型数据（如 PAPER:10001）与 {realm_icon} 占位
    name: "&6突 破"
    amount: 1                # 可选：物品堆叠数（1~64，支持表达式/占位，如 "{realm_order}"）
    glow: true               # 可选：附魔光效（隐藏附魔文字）
    lore:
      - "&7满足条件后点击尝试突破"
    view-requirement:        # 可选：条件全满足才显示该格（语法同突破条件），点击同样受限
      - "realm >= zhuji_qianqi"
    actions:                 # 点击动作，键可用 left-click / right-click / shift-left-click / shift-right-click / middle-click
      left-click:
        - "breakthrough"
  "D":
    material: PLAYER_HEAD
    skull-owner: "{player}"  # 可选：头颅皮肤主人（material 需为 PLAYER_HEAD）
    name: "&b我的信息"
```

## 文本占位

- 名称/描述/标题支持：内部 `{占位}`（`{realm_display}` `{progress}` `{var:xxx}` 等，见《突破与条件》）+ `%PAPI%`
- 特殊 lore 行 `{condition_lines}`：自动展开为当前境界的全部突破条件进度（多行）
- 材质支持 `{realm_icon}`（当前境界的图标材质）

## 动作 DSL（与突破/渡劫动作通用）

| 动作 | 说明 |
|---|---|
| `breakthrough` | 尝试突破（含渡劫流程） |
| `tribulation` | 独立发起当前境界天劫 |
| `gui: <界面id>` | 打开另一个界面 |
| `close` | 关闭界面 |
| `message:` `broadcast:` `actionbar:` `title:` `sound:` `console:` `player:` `give-item:` `var:` `chance:` `action-group:` `delay:` 等 | 全部动作见《突破与条件》动作表 |

## 境界列表专用（special）

| special | 用途 |
|---|---|
| `realm-list` | 该符号的所有槽位自动填充境界链条目并翻页（每页数量 = 槽位数） |
| `prev-page` / `next-page` | 翻页按钮 |
| `top-list` | 槽位填充境界排行前 N 名（N = 槽位数，不翻页；数据与 `/xiuxian top` 同一份 60 秒缓存，v1.4.0） |

`top-list` 条目里可用的局部占位：`{entry_rank}`（名次）、`{entry_name}`（玩家名）、`{entry_realm_display}`（境界显示名）、`{entry_icon}`（境界图标，用在 material）。

`realm-list` 条目里可用的局部占位：

| 占位 | 内容 |
|---|---|
| `{entry_display}` | 该境界显示名 |
| `{entry_id}` / `{entry_order}` / `{entry_index}` | 节点 ID / 排序值 / 序号 |
| `{entry_icon}` | 该境界图标（用在 material） |
| `{entry_current}` | 当前境界标记（非当前境界时该 lore 行自动隐藏；文案在 messages.yml `gui.current-marker`） |

## 行为细节

- 界面内所有点击都被拦截，物品拿不出来；动作执行后界面自动刷新（进度实时变化）
- `refresh-interval` 大于 0 时界面按间隔自动重渲染，适合放进度条/渡劫倒计时；界面关闭自动停止
- `view-requirement` 不满足的格子不渲染也不响应点击（条件变化后随下次刷新出现/消失）
- 玩家背包区域的点击不触发动作
- 界面 id 不存在时提示 `gui.not-found`
