# 打坐修炼

> 这页解决：打坐怎么玩、收益怎么配、灵脉（修炼加成区域）怎么建。v1.4.0 起可用。

## 玩家怎么玩

**蹲下（潜行）保持不动**，默认 3 秒后进入打坐；之后每隔几秒自动获得修炼收益（默认发灵气变量 + 粒子特效）。**移动、起身、受到伤害、开始渡劫、下线**都会退出打坐。站在灵脉范围内打坐，收益按灵脉倍率放大。

- 渡劫过程中无法进入打坐
- 打坐状态不存档（重进服务器要重新打坐）
- 累计打坐时长记入计数器 `cultivation:seconds`，可做条件与展示（见下）

## config.yml 配置（全局）

```yaml
cultivation:
  enabled: true              # 总开关
  enter-sneak-seconds: 3     # 蹲下静止满 N 秒进入打坐
  tick-interval-seconds: 5   # 收益结算间隔（秒）
  reward-actions:            # 每次结算执行的动作（动作 DSL，见《突破与条件》）
    - "var: add lingqi 1 * {multiplier}"
    - "particle: END_ROD:6:0.5"
  enter-actions: []          # 进入打坐时执行
  exit-actions:              # 退出打坐时执行
    - "actionbar: &7你结束了打坐。"
```

`{multiplier}` 在打坐相关动作里可用，值为所在灵脉的倍率（不在灵脉时为 1）。

## 按境界覆盖收益

境界文件里写 `cultivation.reward-actions`，该境界的玩家打坐时用它替代全局收益（小境界内同样可再覆盖）：

```yaml
# 境界/全部境界.yml
jindan:
  order: 30
  display: "&6金丹期"
  cultivation:
    reward-actions:
      - "var: add lingqi 5 * {multiplier}"   # 金丹期修炼更快
      - "particle: WAX_OFF:10:0.6"
```

## 灵脉（修炼加成区域）

### 方式一：`灵脉/` 文件夹配置

首次启动释出 `_示例灵脉.yml`（去下划线并改坐标后启用）：

```yaml
houshan:
  display: "&a后山灵脉"
  zone: "world,100,64,200,20"   # 世界,x,y,z,半径（球形范围）
  multiplier: 2.0               # 打坐收益倍率
  description:
    - "&7后山地气汇聚之处"
```

### 方式二：游戏内指令（推荐）

站到目标位置执行（存 `数据/灵脉区域.yml`，与文件夹配置合并生效）：

```
/xiuxian admin lingmai add houshan 20 2.0     # 以脚下为中心，半径 20，倍率 2.0
/xiuxian admin lingmai remove houshan          # 只能移除指令添加的
/xiuxian admin lingmai list                    # 列出全部（标注哪些是动态添加）
```

- 玩家同时处于多条灵脉时取**最大倍率**
- 进入打坐时会提示所在灵脉名与倍率（文案在 messages.yml `cultivation.start-lingmai`）

## 占位符与计数

| 占位 | 内容 |
|---|---|
| `{cultivating}` / `%xiuxian_cultivating%` | 是否在打坐（1/0） |
| `{cultivation_multiplier}` / `%xiuxian_cultivation_multiplier%` | 当前位置灵脉倍率 |
| `{counter:cultivation:seconds}` / `%xiuxian_counter_cultivation:seconds%` | 累计打坐秒数 |

实战示例——突破条件要求累计打坐 1 小时：

```yaml
breakthrough:
  conditions:
    - "expr {counter:cultivation:seconds} >= 3600"
```

## 开发者

- 事件：`CultivationStartEvent`（可取消）、`CultivationEndEvent`（原因 MOVE/DAMAGE/SNEAK_OFF/QUIT/TRIBULATION/RELOAD），见《开发者API》
- AX 界面：变量 `xx_cultivating`、`xx_cultivation_multiplier`，事件包 `cultivation_start` / `cultivation_end`，见《AX界面对接》
