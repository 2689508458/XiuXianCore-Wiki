# 配置总览

> 这页解决：config.yml 每一项是什么、配置文件夹的通用规则、以及改完配置怎么生效。

## 通用规则（所有配置文件夹通用）

1. **编码一律 UTF-8**（用 VSCode/Notepad++ 编辑；不要用 Windows 记事本的 ANSI 编码保存）
2. 配置文件夹（境界/渡劫/变量/界面/AX界面）**递归扫描**所有 `.yml`，可以自己建子文件夹分组
3. 以 `_` 开头的文件与文件夹会被**忽略**——当草稿位/停用位用
4. **文件名随意（可中文）**，只做组织用途；对象身份 = 文件内的 `id` 字段
5. `id` 只能用**英文字母、数字、下划线**；展示文字（`display`/`description`）随意用中文和颜色码
6. 单个文件写错只会跳过该文件（控制台有中文报错），不影响其他文件
7. 改完配置执行 `/xiuxian admin reload` 即时生效，**唯一例外：storage 存储段需要重启**
8. **一个文件可以写多个对象**（境界/渡劫/变量/界面/AX界面 通用）：
   - 单对象格式（默认示例）：文件顶层直接写 `id:` 等字段
   - 多对象格式：不写顶层 `id`，每个**顶层键就是一个对象的 id**（英文），字段写在键下面：

   ```yaml
   # 境界/全部境界.yml —— 一个文件写完三个大境界
   lianqi:
     order: 10
     display: "&a炼气期"
     sub-realms: [ {id: qianqi, display: "&a炼气期·前期"} ]
   zhuji:
     order: 20
     display: "&b筑基期"
   jindan:
     order: 30
     display: "&6金丹期"
   ```

## 文本颜色写法

- 支持 `&` 颜色码：`"&b筑基期"`
- 不含 `&` 且带 `<标签>` 的文本按 MiniMessage 解析：`"<gradient:blue:aqua>筑基期</gradient>"`

## config.yml 全项说明

```yaml
storage:
  type: SQLITE          # 存储类型：YAML | SQLITE | MYSQL（详见《存储与迁移》）
  auto-save-seconds: 300
  mysql:
    host: 127.0.0.1
    port: 3306
    database: xiuxian
    user: root
    password: ""
    table-prefix: xiuxian_
    pool-size: 8

arcartx:
  enabled: true          # 是否启用 ArcartX 对接（阶段6 启用）
  variable-prefix: "xx_" # 同步到客户端的服务端变量前缀

tribulation:
  blocked-commands: ["/tp", "/home", "/spawn"]  # 渡劫中禁用的指令（阶段4 启用）

debug: false             # 调试日志；也可用 /xiuxian admin debug 临时切换
```

## messages.yml

- 所有玩家可见文案都在这里，键为英文、文案随意改
- `{xxx}` 是占位符（如 `{cost}`、`{player}`），保留原样即可
- 删掉某个键会回退到插件内置默认值
