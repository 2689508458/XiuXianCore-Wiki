# 配置总览

> 这页解决：config.yml 每一项是什么、配置文件夹的通用规则、以及改完配置怎么生效。

## 通用规则（所有配置文件夹通用）

1. **编码一律 UTF-8**（用 VSCode/Notepad++ 编辑；不要用 Windows 记事本的 ANSI 编码保存）
2. 配置文件夹（境界/渡劫/变量/界面/AX界面/动作组/灵脉）**递归扫描**所有 `.yml`，可以自己建子文件夹分组
3. 以 `_` 开头的文件与文件夹会被**忽略**——当草稿位/停用位用
4. **文件名随意（可中文）**，只做组织用途；对象身份 = 文件内的 `id` 字段
5. `id` 只能用**英文字母、数字、下划线**；展示文字（`display`/`description`）随意用中文和颜色码
6. 单个文件写错只会跳过该文件（控制台有中文报错），不影响其他文件
7. 改完配置执行 `/xiuxian admin reload` 即时生效（也可 `reload <模块>` 只重载某一类，见《指令与权限》），**唯一例外：storage 存储段需要重启**
8. **一个文件可以写一个或多个对象**，两种写法见下节

## 配置文件的两种写法（境界/渡劫/变量/界面/AX界面/动作组/灵脉 通用）

**判定规则一句话：文件顶层有 `id:` 字段 = 单对象；没有 = 多对象（每个顶层键就是一个对象的 id）。** 两种格式可在同一文件夹混用。

### 写法一：单文件 · 单对象

一个文件只装一个对象，字段全部写在顶层（适合喜欢一物一档的排版）：

```yaml
# 境界/筑基期.yml
id: zhuji            # ← 顶层有 id，整个文件就是这一个境界
order: 20
display: "&b筑基期"
sub-realms:
  - id: qianqi
    display: "&b筑基期·前期"
attributes:
  - "攻击力: 15"
breakthrough:
  conditions: [ "level >= 15" ]
  costs: [ "level 15" ]
```

### 写法二：单文件 · 多对象

不写顶层 `id`，每个**顶层键就是对象 id**（英文），该对象的字段整体缩进一层：

```yaml
# 境界/全部境界.yml —— 一个文件写完多个境界
lianqi:              # ← 顶层键 = 境界 id
  order: 10
  display: "&a炼气期"
  sub-realms:
    - id: qianqi
      display: "&a炼气期·前期"
  breakthrough:
    conditions: [ "level >= 5" ]
    costs: [ "level 5" ]

zhuji:               # ← 第二个境界接着写
  order: 20
  display: "&b筑基期"
  attributes: [ "攻击力: 15" ]

jindan:              # ← 第三个，字段随意精简
  order: 30
  display: "&6金丹期"
```

渡劫/变量/动作组同理，例如 `渡劫/全部天劫.yml` 顶层写 `leijie_3:`、`leijie_6:` 各配一套；`变量/全部变量.yml` 顶层写 `linggen:`、`lingqi:`；`动作组/_示例动作组.yml` 顶层写各组 id（插件自带示例就是多对象格式）。

注意事项：

- 多对象格式的顶层键就是 id，**必须英文**；想键名与 id 不同可在节内写 `id:` 覆盖
- 对象归属哪个文件、文件内先后顺序都不影响逻辑（境界顺序只看 `order`）
- 某个对象写错只跳过该对象（报错精确到 `文件名 › 键名`），不影响同文件其他对象

## 文本颜色写法

- 支持 `&` 颜色码：`"&b筑基期"`
- 不含 `&` 且带 `<标签>` 的文本按 MiniMessage 解析：`"<gradient:blue:aqua>筑基期</gradient>"`

## config.yml 全项说明

```yaml
storage:
  type: SQLITE          # 存储类型：YAML | SQLITE | MYSQL（详见《存储与迁移》）
  auto-save-seconds: 300
  mysql:
    host: 127.0.0.1
    port: 3306
    database: xiuxian
    user: root
    password: ""
    table-prefix: xiuxian_
    pool-size: 8

arcartx:
  enabled: true          # 是否启用 ArcartX 对接
  variable-prefix: "xx_" # 同步到客户端的服务端变量前缀

tribulation:
  blocked-commands: ["/tp", "/home", "/spawn"]  # 渡劫中禁用的指令

cultivation:               # 打坐修炼（详见《打坐修炼》）
  enabled: true
  enter-sneak-seconds: 3   # 蹲下静止满 N 秒进入打坐
  tick-interval-seconds: 5 # 收益结算间隔（秒）
  reward-actions:          # 每次结算执行的动作；{multiplier} = 灵脉倍率
    - "var: add lingqi 1 * {multiplier}"
    - "particle: END_ROD:6:0.5"
  enter-actions: []
  exit-actions:
    - "actionbar: &7你结束了打坐。"

debug: false             # 调试日志；也可用 /xiuxian admin debug 临时切换
```

> v1.4.1 已修复默认 `var:` 收益动作对 `{multiplier}` 的解析；v1.4.0 的临时规避方式见《[v1.4.0 / v1.4.1 更新指南](v1.4.0更新指南.md)》。

## messages.yml

- 所有玩家可见文案都在这里，键为英文、文案随意改
- `{xxx}` 是占位符（如 `{cost}`、`{player}`），保留原样即可
- 删掉某个键会回退到插件内置默认值
