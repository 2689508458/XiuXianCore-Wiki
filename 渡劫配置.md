# 渡劫配置

> 这页解决：怎么写一个天劫、怎么让某个境界必须渡劫、渡劫全流程与各种边界情况。

## 突破与渡劫的关系（先看这个）

- **突破 = 升境界的完整流程**；**渡劫 = 突破中的一道可选关卡**（境界配了 `tribulation` 才有）
- 玩家日常只需要点"突破"——需要渡劫时天劫会自动降临，渡劫成功才升境界
- **独立发起的渡劫不会升级**：GUI 的"渡劫"按钮 / `tribulation` 动作 / `admin tribulation start` 都是只走天劫流程（雷击/召唤/成功率/失败惩罚），成功也不升境界、不扣突破消耗——定位是历练/试炼/测试。不需要的话把主界面的渡劫按钮删掉即可

## 渡劫流程

```
玩家 breakthrough → 条件全达成 → 消耗全够 → 扣除消耗 → 天劫降临（BossBar 计时）
  → 按波次落雷/召唤 MM 怪 → 熬过全部波次 → 按 success-chance 判定
      成功 → 完成突破（升境界）→ 执行天劫 on-success
      失败 → 扣除惩罚等级 → 执行天劫 on-fail（消耗不退还）
  途中死亡 / 超时 → 直接失败；掉线按 offline-policy 处理
```

**注意**：突破消耗在**发起渡劫时**就扣除，渡劫失败不退还——渡劫本身就是一次投入。

## 让境界需要渡劫

在境界文件的 `breakthrough` 段填天劫 id：

```yaml
breakthrough:
  tribulation: leijie_3     # 渡劫/ 文件夹中某个天劫的 id
  conditions: [ "level >= 15" ]
  costs: [ "level 15" ]
```

## 天劫文件全字段（`渡劫/` 每文件一个）

```yaml
id: leijie_3
display: "&5三重雷劫"
time-limit-seconds: 120     # 总时限，超时=失败（最小 10）

waves:                      # 至少一波；delay-ticks 为距上一波的间隔（20tick=1秒）
  - delay-ticks: 60
    lightning-count: 3      # 本波雷击次数（每 8tick 落一道，视觉闪电+真实伤害）
    lightning-damage: "{max_health} * 0.2"   # 每道雷的伤害，支持表达式
    summon-mobs: ["mm:LeiJieShou * 2"]       # 可选：召唤 MM 怪（会话结束自动清理残留）
  - delay-ticks: 100
    lightning-count: 5
    lightning-damage: "{max_health} * 0.3"

success-chance: "60 + {var:linggen} * 5"     # 0~100，支持表达式；>=100 必成

on-success: [ "message: &a天劫已过！" ]       # 动作 DSL，同突破动作
on-fail: [ "message: &c道行受损……" ]

penalty:
  levels: 10                # 失败扣除的原版等级

restrict:
  block-commands: true      # 渡劫中禁用指令（黑名单在 config.yml tribulation.blocked-commands）

offline-policy: FAIL        # FAIL=掉线判失败并惩罚 | PAUSE=中止不惩罚（需重新发起）
pay-costs: on-start         # 消耗时机：on-start=降临时扣失败不退（默认）| on-success=成功后才扣
```

**关于"失败扣两遍"**：默认（on-start）失败时损失 = 突破消耗（境界文件 costs） + 天劫惩罚（penalty.levels），是两笔账。
想失败只扣惩罚就把 `pay-costs` 改成 `on-success`（成功时会重新校验消耗，期间把材料丢了则突破作废）。

## 边界行为一览

| 情况 | 结果 |
|---|---|
| 渡劫中死亡 | 失败（惩罚生效） |
| 超过 time-limit-seconds | 失败 |
| 掉线 | 按 `offline-policy`：FAIL 失败 / PAUSE 无惩罚中止 |
| 关服 | 同掉线策略 |
| 管理员 cancel | 无惩罚中止 |
| 渡劫中使用被禁指令 | 拦截并提示 |
| 同时发起第二场 | 拒绝（提示渡劫中） |

## 相关指令与占位符

| 指令 | 说明 |
|---|---|
| `/xiuxian admin tribulation start <玩家> [天劫id]` | 强制发起（省略 id 用玩家当前境界配置的天劫；独立发起不含突破） |
| `/xiuxian admin tribulation cancel <玩家>` | 无惩罚取消 |
| `/xiuxian admin region add <天劫id> [半径]` | **站在目标位置执行**，把脚下设为渡劫地点（默认半径 15，自动开启粒子预览） |
| `/xiuxian admin region show <天劫id>` | 切换该天劫全部地点的边界粒子显示（60 秒，需在地点 100 格内） |
| `/xiuxian admin region list <天劫id>` | 列出全部地点（配置文件里的 + 游戏内 add 的） |
| `/xiuxian admin region remove <天劫id> <序号>` | 移除游戏内添加的地点（配置文件里的请直接改文件） |

**渡劫地点说明**：
- 游戏内 `region add` 添加的地点存在 `数据/渡劫区域.yml`（插件自管理，不会动你的天劫配置文件），与天劫文件 `restrict.regions` 里的**合并生效**
- 只要配置了任意地点，玩家就必须站在某个地点内才能引动天劫，否则提示"此地灵气不足"
- **渡劫进行中**会持续向渡劫者绘制所在地点的边界粒子（END_ROD 圆环 + 中心光柱）

| 占位符 | 内容 |
|---|---|
| `%xiuxian_dujie_state%` | `active`（渡劫中）/ `idle` |
| `%xiuxian_dujie_wave%` | 当前波次（未开始为 0） |
| `%xiuxian_dujie_time%` | 剩余秒数 |

> GUI/AX 界面动作里可用 `tribulation` 动作独立发起当前境界天劫（不含突破，仅执行天劫自身流程）。
