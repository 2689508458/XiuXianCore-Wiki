# 突破与条件

> 这页解决：突破条件/消耗怎么写、支持哪些条件类型、表达式语法、击杀计数规则。

## 突破流程

玩家执行 `/xiuxian breakthrough`（或点 GUI 按钮）后：

```
校验 conditions（全部满足）→ 校验 costs（全部足够）→ 渡劫（如有配置）→ 原子扣除 costs → 升到下一境界 → 执行 on-success 动作
任一步不满足 → 提示未达成项 + 执行 on-fail 动作
```

> 若境界配置的天劫 id 在 `渡劫/` 里不存在，会**阻止突破**并提示玩家联系管理员（控制台同时输出配置错误），不会静默放行。

条件与消耗写在**当前境界节点**的 `breakthrough` 段里（见《境界配置》）。

## 条件 DSL（conditions，一行一条）

| 类型 | 写法示例 | 说明 |
|---|---|---|
| 等级 | `level >= 30` | 原版等级 |
| 击杀原版怪 | `kill vanilla:ZOMBIE >= 50` | 类型为原版实体名 |
| 击杀 MM 怪 | `kill mm:leijie_shou >= 10` | MM 内部名；需装 MythicMobs |
| 击杀（附加项） | `kill mm:leijie_shou scope:realm show:雷劫兽 >= 10` | `scope:realm`=只统计进入当前境界之后的击杀；`show:`=进度里的显示名 |
| 持有物品 | `item material:BLAZE_POWDER >= 5` | 只判定不扣除；扣除写进 costs |
| 持有物品（组合） | `item material:PAPER model:10001 name:聚灵丹 >= 5` | 参数可组合：材质+模型数据+名称包含 |
| 持有 MM 物品 | `item mm:juling_dan >= 5` | 按 MM 物品内部 ID 识别 |
| 金币 | `money >= 10000` | 需 Vault + 经济插件 |
| 点券 | `points >= 100` | 需 PlayerPoints |
| 权限 | `permission xiuxian.vip` | |
| 变量 | `var linggen >= 3` | 见《变量系统》 |
| PAPI | `papi %player_health% >= 15` | 任意 PAPI 占位符；非数值时支持 == / != 字符串比较 |
| 表达式 | `expr ({var:linggen} * 2 + {level}) >= 60` | 完整四则运算，见下 |
| 境界 | `realm >= jindan_qianqi` | 按境界链先后位置比较，右侧写境界节点 id |
| 世界 | `world world,world_the_end` | 所在世界名，逗号分隔任一匹配 |
| 群系 | `biome plains,minecraft:forest` | 所在群系 id，逗号分隔任一匹配 |
| 时间 | `time day` / `time night` | 所在世界白天/夜晚 |
| 在线时长 | `playtime >= 600` | 累计在线分钟数（原版统计） |
| 突破冷却 | `cooldown 3600` | 距进入当前境界需满的秒数（防连续突破） |
| 任一满足 | `any level >= 50 \| permission xiuxian.vip` | 子条件用 `\|` 分隔，满足其一即通过 |

- 比较符支持 `>= <= == != > <`；**比较符右侧都可以写表达式**（如 `level >= {var:linggen} * 5`）
- 缺前置时对应条件判定为不满足，进度文本会带"[缺少前置 xxx]"标记
- 条件写错不会崩：加载时控制台输出中文错误并忽略该行
- **物品条件只统计主背包 36 格**（不含盔甲栏与副手），消耗扣除同理
- 管理员可用 `/xiuxian admin condition test <玩家> <条件行>` 在线调试任意条件写法

## 消耗 DSL（costs，一行一条）

| 类型 | 写法示例 | 说明 |
|---|---|---|
| 等级 | `level 30` | 扣原版等级 |
| 经验点 | `exp 5000` | 扣原版经验点（不整级扣） |
| 物品 | `item mm:juling_dan 5`、`item material:PAPER model:10001 5` | 数量写在最后 |
| 金币 | `money 10000` | |
| 点券 | `points 100` | |
| 变量 | `var lingqi 1000` | 扣数值型变量 |

- **先全部校验再统一扣除**：任何一项不够就一项都不扣
- 数量同样支持表达式（如 `level {realm_order} + 10`）

## 表达式语法

- 运算符：`+ - * / % ^`、括号、一元负号 `-`、逻辑非 `!`
- 比较：`> >= < <= == !=`（结果 1/0）；逻辑：`&& ||`
- 函数：`min(a,b,…)` `max(a,b,…)` `abs(x)` `floor(x)` `ceil(x)` `round(x)` `sqrt(x)`、
  `random()`（0~1 随机）、`random(a,b)`（a~b 随机）——如渡劫成功率 `min(90, 50 + {var:linggen} * 5)`
- 可用占位符（也可在动作文本中使用）：

| 占位符 | 含义 |
|---|---|
| `{level}` `{exp}` | 原版等级 / 累计经验 |
| `{health}` `{max_health}` | 当前/最大生命值 |
| `{player}` | 玩家名（文本场景用） |
| `{realm}` `{realm_display}` `{realm_major}` `{realm_order}` | 当前境界信息 |
| `{next_realm}` `{next_realm_display}` | 下一境界 |
| `{var:id}` `{gvar:id}` | 玩家/全局变量 |
| `{kill:mm:id}` `{kill:vanilla:类型}` | 击杀计数 |
| `{progress}` `{progress_bar}` | 突破进度百分比 / 10 格文本进度条 |
| `{dujie_state}` `{dujie_wave}` `{dujie_time}` | 渡劫状态（active/idle）/ 当前波次 / 剩余秒 |
| `%任意papi%` | 需装 PlaceholderAPI |

- 求值失败会在控制台输出中文错误，并附"原始表达式 + 替换后内容"，方便定位没替换成功的占位符

## 动作 DSL（on-success / on-fail / 天劫 / GUI 点击 / AX 回调共用）

突破成功/失败、渡劫成败、界面按钮都可以执行动作列表，**支持执行任意命令**：

| 动作 | 说明 |
|---|---|
| `console: <命令>` | 以控制台执行（发权限/给钱/接其他插件都用它） |
| `player: <命令>` | 以玩家身份执行 |
| `message:` / `broadcast:` / `actionbar:` | 私聊 / 全服广播 / 物品栏上方提示 |
| `title: 主标题\|副标题[\|淡入\|停留\|淡出]` | 屏幕标题，后三段为 tick（可省略） |
| `sound: 名称[:音量[:音调]]` | 音效；支持 `ENTITY_PLAYER_LEVELUP`、`minecraft:entity.player.levelup` 与资源包自定义 `custom:my.sound` |
| `give-item: mm:<id>\|材质[:模型] [数量]` | 给物品（支持 MM 物品与自定义模型数据） |
| `give-exp: <表达式>` / `give-levels: <表达式>` | 给经验点 / 给等级 |
| `potion: 效果:秒[:等级]` | 药水效果（原版效果 id，如 `speed:10:2`） |
| `heal: max\|<表达式>` | 回满血 / 恢复指定血量 |
| `particle: 名称[:数量[:偏移]]` | 粒子（如 `TOTEM_OF_UNDYING:60:1`） |
| `firework: [威力]` | 随机颜色庆祝烟花（威力 1~3） |
| `teleport: 世界,x,y,z[,yaw,pitch]` | 传送 |
| `var: <set\|add\|take> <变量id> <表达式>` | 修改触发玩家的变量（无需绕 console 指令） |
| `gvar: <set\|add\|take> <变量id> <表达式>` | 修改全局变量 |
| `chance: <百分比表达式>` | 概率闸门：未命中则**终止后面所有动作**（如 `chance: 30`） |
| `action-group: <组id>` | 执行 `动作组/` 里定义的动作组（见下节） |
| `delay: <tick>` | 延迟后续动作（20tick=1秒） |
| `gui: <界面id>` / `close` | 打开界面 / 关闭界面 |
| `breakthrough` / `tribulation` | 触发突破 / 独立发起天劫 |

## 动作组（复用动作列表）

把常用的动作列表写进 `plugins/XiuXianCore/动作组/` 文件夹（首次启动会释出 `_示例动作组.yml`，去掉文件名下划线即启用），任何动作列表里用 `action-group: <组id>` 引用：

```yaml
# 动作组/庆祝.yml —— 顶层键即组 id，一个文件可写多个组
celebrate_breakthrough:
  actions:
    - "firework: 2"
    - "sound: ENTITY_PLAYER_LEVELUP"
    - "title: &6✦ 境界突破 ✦|&f{realm_display}|10|60|20"
```

```yaml
# 境界文件里引用
breakthrough:
  on-success:
    - "action-group: celebrate_breakthrough"
    - "chance: 10"
    - "broadcast: &d{player} 福缘深厚，天降异象！"   # 只有 10% 概率广播
```

动作组内可以再引用其他动作组（嵌套上限 8 层，防止互相引用死循环）。改完文件用 `/xiuxian admin reload actions` 单独重载。

完整示例（写在境界文件里）：

```yaml
breakthrough:
  on-success:                    # 突破成功后执行（此时已是新境界）
    - "console: give {player} diamond 3"
    - "console: lp user {player} parent add zhuji"   # 同步权限组
    - "broadcast: &6{player} 突破至 {next_realm_display}&6！"
    - "title: &b境界突破|&f{next_realm_display}"
    - "sound: ENTITY_ENDER_DRAGON_GROWL"
    - "delay: 40"
    - "message: &7新的修行之路开始了……"
  on-fail:                       # 条件/消耗不足时执行
    - "message: &c时机未到。"
```

文本可用 `{player}` `{realm_display}` `{old_realm_display}` `{next_realm_display}` `{var:xxx}` 及任意 `%PAPI%`。

## 击杀计数规则

- 原版击杀与 MM 击杀**分开计**：打死一只 MM 怪会同时 +1 `kill:vanilla:<实体类型>` 和 +1 `kill:mm:<MM内部名>`
- 计数持久化存储，跨重启累计；`scope:realm` 的条件按"进入当前境界那一刻起"的增量计算（首次查询时建立基线）
- 管理员可用 `/xiuxian admin counter <get|set|add> <玩家> <计数键> [值]` 查改计数（键如 `kill:mm:leijie_shou`），或 `/xiuxian admin data <玩家>` 查看全量数据
