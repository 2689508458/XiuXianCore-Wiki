# v1.4.0 / v1.4.1 更新指南

> 这页解决：从旧版本升级到 v1.4.x 时要改什么，以及如何快速启用打坐、灵脉、突破成功率、走火入魔和境界排行榜。

## v1.4.1 补丁

v1.4.1 修复了 v1.4.0 的两项问题：

- `var:` / `gvar:` 动作现会解析打坐传入的 `{multiplier}`，默认灵气收益可以正确按灵脉倍率计算
- 新增 `on-chance-fail`，可把走火入魔限制在突破成功率未命中时执行

## 新增内容一览

| 功能 | 用途 | 详细文档 |
|---|---|---|
| 打坐修炼 | 玩家蹲下静止后周期获得修炼收益 | [打坐修炼](打坐修炼.md) |
| 灵脉区域 | 在指定区域放大打坐收益 | [打坐修炼](打坐修炼.md) |
| 突破成功率 | 为每个境界单独配置突破成功概率 | [突破与条件](突破与条件.md) |
| 走火入魔 | 突破失败时按概率降低境界 | [突破与条件](突破与条件.md) |
| 境界排行榜 | 通过指令、PAPI 或箱子 GUI 展示全服排名 | [界面配置](界面配置.md) |
| 通用计数占位符 | 读取打坐时长等任意持久化计数 | [占位符](占位符.md) |

## 从旧版本升级

1. 停服并备份 `plugins/XiuXianCore/`。
2. 替换插件 jar 后重新启动服务器。
3. 旧的 `config.yml` **不会自动补入**新增的 `cultivation` 段。需要打坐收益时，请手动加入下面的默认配置。
4. 新的 `messages.yml` 文案可以从 jar 内置默认值回退读取；只有需要自定义文案时才要手动补入。
5. 首次升级会创建 `灵脉/` 并释出停用状态的 `_示例灵脉.yml`。文件名以下划线开头时不会加载。
6. 使用 `/xiuxian version` 确认当前版本为 `1.4.1`。

```yaml
cultivation:
  enabled: true
  enter-sneak-seconds: 3
  tick-interval-seconds: 5
  reward-actions:
    - "var: add lingqi 1 * {multiplier}"
    - "particle: END_ROD:6:0.5"
  enter-actions: []
  exit-actions:
    - "actionbar: &7你结束了打坐。"
```

> 如果旧配置未补入 `reward-actions`，玩家仍可进入打坐，但不会获得周期收益。

> 仍在使用 v1.4.0 时，可把默认收益中的 `{multiplier}` 临时改为 `{cultivation_multiplier}`：
>
> ```yaml
> - "var: add lingqi 1 * {cultivation_multiplier}"
> ```

## 打坐与灵脉

玩家保持蹲下且不移动，默认 3 秒后进入打坐；移动、起身、受到伤害、开始渡劫或下线都会退出。默认每 5 秒结算一次收益。

管理员可站在目标位置直接创建灵脉：

```text
/xiuxian admin lingmai add houshan 20 2.0
/xiuxian admin lingmai list
/xiuxian admin lingmai remove houshan
```

上面的示例会以管理员脚下为中心创建半径 20、收益倍率 2 倍的动态灵脉。动态灵脉保存在 `数据/灵脉区域.yml`；详细字段和文件配置方式见《[打坐修炼](打坐修炼.md)》。

## 突破成功率与走火入魔

在当前境界节点的 `breakthrough` 段加入 `success-chance`。该字段是百分比表达式，省略时为 100：

```yaml
breakthrough:
  conditions:
    - "level >= 30"
  costs:
    - "level 30"
  success-chance: "min(95, 60 + {var:linggen} * 5)"
  on-fail:
    - "message: &c突破条件尚未满足。"
  on-chance-fail:
    - "message: &c突破失败，道基受损……"
    - "chance: 30"
    - "demote: 1"
```

通常的执行顺序是：校验条件和消耗 → 扣除消耗 → 完成渡劫（如有）→ 判定突破成功率 → 升境界。天劫使用 `pay-costs: on-success` 时会改为渡劫成功后再重新校验并扣除消耗。成功率未命中时不会退还已扣消耗，并执行 `on-chance-fail`。

`chance: 30` 未命中时会终止它后面的动作，因此需要每次都执行的失败动作应放在 `chance:` 前面。示例中的 `demote: 1` 只会有 30% 概率执行，境界最低退到境界链首。

> 为兼容旧配置，省略 `on-chance-fail` 时，成功率未命中仍会回退执行 `on-fail`。从 v1.4.0 升级时，请把只应在概率失败后触发的 `demote:` 等惩罚移到 `on-chance-fail`。

> 天劫配置本身也有 `success-chance`。如果境界突破和天劫两处都配置，玩家需要连续通过两次概率判定。

## 境界排行榜

玩家可直接使用：

```text
/xiuxian top
/xiuxian top 20
```

排行榜按境界链位置从高到低排序；同境界玩家按名称排序。数量默认 10，最小 1、最大 50。指令、PAPI 和 GUI 共用一份 60 秒缓存。

PAPI 占位符：

```text
%xiuxian_top_1_name%
%xiuxian_top_1_realm%
```

箱子 GUI 示例：

```yaml
layout:
  - "#########"
  - "#TTTTTTT#"
  - "#########"
items:
  "T":
    special: top-list
    material: "{entry_icon}"
    name: "&e#{entry_rank} &f{entry_name}"
    lore:
      - "&7境界：{entry_realm_display}"
```

`top-list` 展示数量等于布局中对应符号的槽位数，不提供翻页。缓存首次异步加载时可能暂时为空；界面没有配置 `refresh-interval` 时，重新打开界面即可看到刷新后的榜单。

## 通用计数占位符

v1.4.0 可以读取任意计数器：

```text
{counter:cultivation:seconds}
%xiuxian_counter_cultivation:seconds%
```

上面的占位符表示累计打坐秒数，可用于突破条件、界面、计分板或聊天格式。管理员可通过 `/xiuxian admin counter` 查询或调整计数。

## 相关权限

灵脉管理新增权限：

```text
xiuxian.admin.lingmai
```

该权限默认仅 OP 拥有，也包含在总权限 `xiuxian.admin` 中。
